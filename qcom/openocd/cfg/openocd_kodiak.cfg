adapter driver eud
transport select swd
adapter   speed  7500


if { [info exists CHIPNAME] } {
   set _CHIPNAME $CHIPNAME
} else {
   set _CHIPNAME KODIAK 
}

if { [info exists ENDIAN] } {
   set _ENDIAN $ENDIAN
} else {
   set _ENDIAN little
}

if { [info exists DAP_TAPID] } {
	set _DAP_TAPID $DAP_TAPID
} else {
	set _DAP_TAPID 0x5ba02477 
}


#reset_config srst_only srst_pulls_trst
reset_config trst_and_srst

swd newdap $_CHIPNAME cpu -irlen 15 -expected-id $_DAP_TAPID
dap create $_CHIPNAME.dap -chain-position $_CHIPNAME.cpu -ignore-syspwrupack

set _TARGETNAME $_CHIPNAME.cpu

# ------------ CORE0 ---------------
cti create CTI_APSS_CORE0 -dap $_CHIPNAME.dap -baseaddr 0x87020000 -ap-num 1
target create $_CHIPNAME.cpu0 aarch64 -dap $_CHIPNAME.dap -coreid 0 -dbgbase 0x87010000 -cti CTI_APSS_CORE0 -event reset-assert-post { dap init }
KODIAK.cpu0 configure -event examine-end { 
	KODIAK.cpu0 arp_halt 
}